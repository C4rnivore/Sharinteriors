import { ReactNode } from "react";

export const metadata = {
  title: "Sharinterior | Public projects",
};

export default function PageLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
