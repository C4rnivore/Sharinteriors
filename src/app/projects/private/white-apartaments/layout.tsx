import { ReactNode } from "react";

export const metadata = {
  title: "White apartaments",
};

export default function PageLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
