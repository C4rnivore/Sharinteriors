import { ReactNode } from "react";

export const metadata = {
  title: "Sharinterior | Private projects",
};

export default function PageLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
