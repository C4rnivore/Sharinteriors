import { ReactNode } from "react";

export const metadata = {
  title: "Sharinterior | Contacts",
};

export default function PageLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
