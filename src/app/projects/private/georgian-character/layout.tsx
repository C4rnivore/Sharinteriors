import { ReactNode } from "react";

export const metadata = {
  title: "Georgian character",
};

export default function PageLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
